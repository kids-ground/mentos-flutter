import 'package:flutter/material.dart';

class ProfileModifyPage extends StatelessWidget {
  const ProfileModifyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("프로필 작성 페이지"),
    );
  }
}
